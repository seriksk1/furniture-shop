.form {
  width: 100%;
  max-width: 375px;

  form {
    width: 100%;
  }
}

.selected {
  border-bottom: var(--border-dark-1);
  color: var(--color-dark);
}

.notSelected {
  border-bottom: var(--border-transparent-1);
  color: var(--color-gray);
}
